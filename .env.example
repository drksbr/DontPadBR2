# Y-Sweet connection string
# Local development: ys://127.0.0.1:8080
# Docker: ys://ysweet:8080
CONNECTION_STRING=ys://ysweet:8080

# Y-Sweet URL pública para WebSocket (OBRIGATÓRIO para HTTPS)
# Esta é a URL que o cliente (navegador) usará para conectar via WebSocket
# Deve usar wss:// para sites HTTPS, caso contrário o navegador bloqueará a conexão
# Exemplo: wss://api.seu-dominio.com.br
YSWEET_URL_PREFIX=wss://api.dontpad.com.br
