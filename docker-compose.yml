services:
  # Next.js application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:3000"
    environment:
      - NODE_ENV=production
      - CONNECTION_STRING=ys://ysweet:8080
    volumes:
      - app-data:/app/.data
    depends_on:
      - ysweet
    restart: unless-stopped
    networks:
      - dontpad-network

  # Y-Sweet collaboration server
  ysweet:
    build:
      context: .
      dockerfile: Dockerfile.ysweet
    # Porta não exposta externamente - apenas acessível pela rede Docker interna
    expose:
      - "8080"
    volumes:
      - ysweet-data:/data
    restart: unless-stopped
    networks:
      - dontpad-network

networks:
  dontpad-network:
    driver: bridge

volumes:
  ysweet-data:
  app-data:
